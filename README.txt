Bells Plumbing — Cohesive Local Site (clean rebuild)

This rebuild removes the previous overlay/cursor artifacts that came from the
browser-automation layer (the orange cursor and pulsing screen border).

Open index.html, or serve the folder:
  cd bells-plumbing-site && python3 -m http.server 8000
  open http://localhost:8000

Zip: right-click 'bells-plumbing-site' -> Compress (Finder) or
     zip -r bells-plumbing-site.zip bells-plumbing-site (Terminal).
