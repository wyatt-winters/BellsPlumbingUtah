# Block direct web access to bot internals
<FilesMatch "^(config\.php|schedule\.json|posts\.json|publish\.log|bot-state\.json|bot\.log)$">
  Require all denied
</FilesMatch>

<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteRule ^lib/ - [F,L]
</IfModule>
